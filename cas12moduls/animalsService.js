"use strict";

// export default getFemaleAnimals = (array) => {
//   return array.filter((animal) => animal.gender === "female");
// };

// export const getFemaleAnimals = (array) => {
//   return array.sort((a, b) => a - b);
// };
